Always use TypeScript.

Never use JavaScript.

Never use any.

Always create interfaces.

Never duplicate code.

Always use reusable components.

Always validate forms with Zod.

Always use React Hook Form.

Always use TanStack Query.

Never fetch inside components when a custom hook is appropriate.

Always create loading.tsx.

Always create error.tsx.

Always use Suspense.

Always use Server Components whenever possible.

Client Component only when necessary.

Keep components below 150 lines.

Split reusable logic into hooks.

Follow ESLint.

Follow Prettier.

Never hardcode data.

Never leave TODO comments.

Always optimize images.

Always use environment variables.

Use absolute imports.

Create reusable UI components.

Maintain accessibility.

Responsive first.

Never break the design system.